{% extends 'templates/app.twig' %}

{% block title %}{{ products.first['sub-category'] }}{% endblock %}

{% block content %}
	<div class="row">
		<div class="col-md-12 col-sm-12">
			<a href="{{ path_for('home') }}">
				<button class="btn btn-default btn-sm" type="button">{{ products.first.category }} <span><i class="fa fa-times" aria-hidden="true"></i></span></button>
			</a>
			<a href="{{ path_for('category.get', {category: products.first.category}) }}">
				<button class="btn btn-default btn-sm" type="button">{{ products.first['sub-category'] }} <span><i class="fa fa-times" aria-hidden="true"></i></span></button>
			</a>
			
			{% if classifications[0] %}
				<div class="dropdown">
					<button class="btn btn-default btn-lg dropdown-toggle" type="button" data-toggle="dropdown">Select Classification <span><i class="fa fa-caret-down" aria-hidden="true"></i></span></button>
					<ul class="dropdown-menu">
						{% for classification in classifications %}
					    	<li>
					    		<a href="{{ path_for('classification.get', {classification: classification.classification}) }}">
					    			{{ classification.classification }}
					    		</a>
					    	</li>
				    	{% endfor %}
				    </ul>
				</div>
			{% endif %}
		</div>
	</div>
	<hr>
	<div class="row">
		{% for index, product in products %}
			<div class="col-md-4 col-sm-6">
				{% include 'products/partials/item.twig' with {product: product} %}
			</div>
			{% if (index+1)%3 == 0 %}
				</div>
				<div class="row">
			{% endif %}
		{% endfor %}	
	</div>
{% endblock %}